function even_or_odd(number) {
    let result;
    let remainder = number % 2;
    if (remainder != 0) {
        return result = "Odd";
    } else if (remainder === 0) {
        return result = "Even";
    }
}
